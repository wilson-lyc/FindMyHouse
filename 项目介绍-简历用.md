# FindMyHouse — 全栈个人租房决策工作台

## 项目简介

FindMyHouse 是一款面向个人的租房决策辅助工具（租房版 CRM），解决租房过程中房源分散、信息混乱、难以比较的痛点：支持将各渠道看中的候选房源手动录入并统一管理，通过「地图可视化 + 通勤计算 + AI 自然语言助手」的组合，帮助用户高效筛选、对比房源并做出理性决策。

## 主要工作

- **全栈独立开发**：独立完成项目的前后端架构设计与全部开发工作，从前端交互、后端 API 到数据库设计形成完整闭环交付；使用 Vue 3 + TypeScript + Vite + Pinia + Element Plus + ECharts + 高德地图 JS API 构建前端，Fastify + Zod + better-sqlite3 构建后端。

- **后端分层架构**：采用 Repository + Service + Mapper + Routes 四层架构，职责清晰、可测试性强；使用 Zod v4 做运行时类型校验，保证 API 输入输出的类型安全；支持图片处理（sharp）、增量数据库迁移与数据导入导出。

- **AI 租房助手**：基于 LangChain + LangGraph 构建完整 Agent 工作流，用 StateGraph 状态图实现「意图分析 → 路由分发 → 工具调用 → 结果收集」全流程；实现 14 个 Agent 工具（房源/地点增删改查、搜索、对比等）；设计前端交互模型，Agent 可通过工具触发前端确认弹窗、结果展示、单选/多选输入，用户确认后再继续执行；搜索结果基于 Haversine 距离与相似度评分智能排序。

- **地图深度集成**：实现高德地图 JS API 的动态加载与安全配置；管理多类型覆盖物（房源/地点标记、路线绘制、信息弹窗、右键菜单）；支持地图点击拾取坐标、视野范围筛选、多模式路线规划（驾车/公交/骑行/步行）与公交等时圈计算，服务端缓存通勤结果减少重复请求。

- **多房源对比与看房日程**：支持 2-4 套房源横向对比（月成本、通勤时间、户型、费用明细等），可自定义对比字段；为房源安排看房日程并提供日历视图，实现基于 Haversine 公式的时间约束看房路线规划算法。

- **数据库与数据可视化**：设计 7 张数据库表（房源、房源图片、地点、看房日程、路线缓存、配置、会话），增量迁移策略支持旧版本平滑升级；使用 ECharts 搭建数据看板，展示房源状态、租金、来源渠道分布及通勤 vs 租金散点图。

- **用户体验打磨**：实现三步骤首次引导流程（导入数据 → 配置服务 → 创建焦点地点）、路由守卫、图片上传与管理、聊天截图分享等细节功能，降低上手门槛。

