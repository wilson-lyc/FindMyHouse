{
  "name": "find-my-house",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend",
    "start": "npm run start --workspace backend",
    "icons:generate": "node scripts/generate-icons.mjs",
    "backend:build-sidecar": "node scripts/build-backend.mjs",
    "tauri:dev": "cargo tauri dev",
    "tauri:build": "npm run icons:generate && npm run backend:build-sidecar && cargo tauri build"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "sharp": "^0.33.0"
  }
}
